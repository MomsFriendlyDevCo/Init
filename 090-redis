#!/bin/bash
# Install the Neovim Nightly from source
# REQUIRES: 007-apt-update

source common

INIT status "Install Redis PPA"
sudo add-apt-repository -y ppa:redislabs/redis

INIT status "Installing Redis Server"
INIT apt-install redis
