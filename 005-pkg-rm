#!/bin/bash
# Removes unneeded default Ubuntu packages

source common

apt-remove --lazy --header --mode purge "Remove silly media players" \
	banshee \
	gmusicbrowser \
	parole \
	rhythmbox \


apt-remove --lazy --header --mode purge "Remove usless crash loggers" \
	apport-symptoms \
	whoopsie \
	zeitgeist \
	zeitgeist-* \


apt-remove --lazy --header --mode purge "Remove stupid APT indexer for Unity / Ubuntu package manager" \
	apt-xapian-index \


apt-remove --lazy --header --mode purge "Remove Ubuntu telemetry" \
	popularity-contest \


apt-remove --lazy --header --mode purge "Remove heavy non-server packages" \
	abiword \


init status "Cleanup orphaned APT packages"
sudo apt autoremove -y --purge
